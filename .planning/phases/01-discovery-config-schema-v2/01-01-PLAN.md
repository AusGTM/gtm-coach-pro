---
phase: 01-discovery-config-schema-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gtm-coach-pro/references/mcp-discovery.md
  - gtm-coach-pro/CONNECTORS.md
  - gtm-coach-pro/skills/gtm-coach/SKILL.md
  - gtm-coach-pro/references/memory-bank.md
autonomous: true
requirements: [DISC-01, DISC-04]
must_haves:
  truths:
    - "Given a connected Drive-capable MCP tool, mcp-discovery.md's documented probe binds it to `~~meeting recording` with `source_kind: drive_folder` — bound by probed capability, with no Drive tool name written as a required literal binding key (DISC-01)"
    - "Every bound recording source carries a `source_kind` (`api` | `drive_folder`) so skills branch at one point (DISC-04)"
    - "One `drive_folder` `recording_sources[]` entry (`config_schema_version: 2`) can be traced end-to-end: CONNECTORS.md lists Drive → mcp-discovery.md binds + persists it → gtm-coach Step 2 reads its `source_kind` → memory-bank.md config layout names `recording_sources[]`"
  artifacts:
    - "gtm-coach-pro/references/mcp-discovery.md: `source_kind` discriminator (values `api` | `drive_folder`) + a minimal `recording_sources[]` v2 example carrying one `drive_folder` entry (`source_kind`, `vendor`, `tool_map`, `id_field: file_id`, `root_folder_id`) + `config_schema_version: 2`"
    - "gtm-coach-pro/CONNECTORS.md: Google Drive / Gemini `Notes by Gemini` listed as a document-store `~~meeting recording` source"
    - "gtm-coach-pro/skills/gtm-coach/SKILL.md: Step 2 states a bound source may be `source_kind: drive_folder` and to follow mcp-discovery.md for it"
    - "gtm-coach-pro/references/memory-bank.md: config.json layout line references `recording_sources[]`"
  key_links:
    - "The `source_kind` vocabulary in mcp-discovery.md must match the branch token used in gtm-coach/SKILL.md Step 2 (drift here breaks binding)"
    - "The `recording_sources[]` shape in mcp-discovery.md must match the config.json layout line in memory-bank.md (drift orphans the persisted mapping)"
---

<objective>
Establish the load-bearing seam for the whole milestone in a single thin end-to-end slice: a
connected Google Drive tool can be bound to the existing `~~meeting recording` category as a new
*shape* of source (not a new category, not a new skill), discriminated by a `source_kind` field,
and persisted into an array-based `config.json` (schema v2). This tracer wires ONE happy path
through every doc layer the phase touches so a contract mismatch surfaces after one commit, not
after the folder ladder, migration, and privacy gate are all fleshed out.

Purpose: prove the `source_kind` + `recording_sources[]` v2 + capability-probe binding contract is
coherent across CONNECTORS.md → mcp-discovery.md → gtm-coach Step 2 → memory-bank.md before
Plans 02/03 expand it.
Output: minimal but production-quality edits to all four Phase-1 files establishing the seam.
Requirements: DISC-01 (capability-probe binding, no hardcoded name), DISC-04 (`source_kind`).
</objective>

## Artifacts this phase produces (full Phase-1 roster)

Tracked here for the whole phase; each item is delivered by the plan noted.

**config.json (schema v2) — documented in mcp-discovery.md, referenced in memory-bank.md**
- `config_schema_version: 2` (Plan 01 seam; migration in Plan 02)
- `recording_sources[]` array replacing singular `recording_source`/`tool_map` (Plan 01 seam)
- per-source `source_kind` (`"api"` | `"drive_folder"`) (Plan 01)
- per-source `vendor`, `tool_map`, `id_field`, `supports_transcripts`, `last_sync` (Plan 01 seam; carried from v1)
- Drive-source fields: `drive_tool`, `root_folder_name`, `root_folder_id`, `legacy_folder_name`, `legacy_folder_id`, `id_field: "file_id"` (Plan 01 seam; full set in Plan 02)

**mcp-discovery.md sections**
- `source_kind` discriminator subsection (Plan 01)
- Drive capability-bucket remap table — `list_calls`/`get_summary`/`get_transcript`/`get_call_detail` → list/search-files, export-doc, file-metadata (Plan 02)
- Folder-resolution candidate-ladder section: `Google Meet/<subfolder>/` → `Legacy Meet Recordings/` → `Meet Recordings/` → user fallback (Plan 01 minimal; full ladder Plan 02)
- config schema v2 block + non-breaking migration-on-read rule (Plan 01 block; migration Plan 02)

**CONNECTORS.md**
- Google Drive / Gemini `Notes by Gemini` as a document-store `~~meeting recording` option (Plan 01) + "what it unlocks" explainer (Plan 02)

**gtm-coach/SKILL.md**
- Step 2 branch by `source_kind` (Plan 01 minimal note; full branch Plan 03)
- Step 3 privacy-gate re-surface scoped to Drive (Plan 03)

**memory-bank.md**
- config.json layout line → `recording_sources[]` (Plan 01)
- Privacy note extended: Drive-scoped re-surface + redact inline comments and non-buying-committee speaker names + scope reads to the resolved recordings folder (Plan 03)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Note: this project is a Claude **skill plugin** — markdown SKILL.md + reference docs, no runtime,
no build, no test suite. "Implementation" is editing markdown. Acceptance is grep-style source
assertions on the markdown plus procedure-level behavior (does the documented procedure, read
top to bottom, produce the required outcome). No phase VALIDATION.md exists for Phase 1 by design
(milestone-level research substitutes) — do not block on its absence.
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "bind one Drive folder as a source_kind: drive_folder source" — one path only</name>
  <files>gtm-coach-pro/references/mcp-discovery.md, gtm-coach-pro/CONNECTORS.md, gtm-coach-pro/skills/gtm-coach/SKILL.md, gtm-coach-pro/references/memory-bank.md</files>
  <read_first>
    @gtm-coach-pro/references/mcp-discovery.md (the discovery/binding contract you are extending — note §1 capability buckets, §2 resolve source, §3 config.json block + probe, §6 privacy gate)
    @gtm-coach-pro/CONNECTORS.md (the category→tool binding doc; `~~meeting recording` options table + "what each connector unlocks")
    @gtm-coach-pro/skills/gtm-coach/SKILL.md (Step 2 discovery, Step 3 privacy gate)
    @gtm-coach-pro/references/memory-bank.md (config.json layout line ~11; Dedup rule; index.json schema)
    @.planning/research/ARCHITECTURE.md ("Concrete Schema Additions" → config.json schema v2; Pattern 1 source_kind; Pattern 4 file-ID dedup)
  </read_first>
  <reversibility rating="costly">Defines the `source_kind` vocabulary (`api`|`drive_folder`) and `recording_sources[]` v2 shape every downstream skill branches on; research already locked these, but changing the discriminator vocabulary later ripples through gtm-coach and sync-memory. Flagged, not gated.</reversibility>
  <action>
    Wire ONE path — a single connected Drive tool bound to `~~meeting recording` as one
    `drive_folder` source — through every doc layer. No migration rule, no full folder ladder, no
    privacy re-surface yet (those are Plans 02/03); just the seam, end to end, production-quality.

    In `mcp-discovery.md`: add a short subsection introducing the `source_kind` discriminator on a
    bound `~~meeting recording` source, with exactly two values — `api` (the existing
    `list_calls`/`get_summary`/`get_transcript`/`get_call_detail` tools) and `drive_folder` (a
    document store you list/search and export). State that a `drive_folder` source is discovered by
    the SAME capability probe used for every other source (§3) — the tool is bound by its probed
    capability shape and its actual tool names are persisted into `tool_map`; do not name any one
    Drive tool as a required binding key (per DISC-01 and the tool-agnostic contract). Then extend
    §3's `config.json` block to schema v2: add `config_schema_version: 2`, replace the singular
    `recording_source`/`tool_map` with a `recording_sources[]` array, and show ONE `drive_folder`
    entry carrying `source_kind: "drive_folder"`, `vendor: "google-drive"`, a `tool_map` of probed
    tool names, `id_field: "file_id"` (the Google Doc Drive file ID — per Pattern 4, never a
    synthesized title+date), `root_folder_id`, and per-source `last_sync`. Add one line that a
    `drive_folder` source resolves its recordings folder by searching known candidate names and
    records the resolved `root_folder_id` (full candidate ladder lands in Plan 02).

    In `CONNECTORS.md`: add Google Drive / Gemini `Notes by Gemini` as a `~~meeting recording`
    option, noting it is a *document store* (list/search/export) rather than a purpose-built
    recording API — a new shape of the existing category, not a new category.

    In `gtm-coach/SKILL.md` Step 2: add one sentence that a discovered source may bind as
    `source_kind: drive_folder` (a Google Drive / Gemini notes folder) and that its discovery and
    folder resolution follow `mcp-discovery.md`.

    In `memory-bank.md`: update the `config.json` layout comment line so it names
    `recording_sources[]` (schema v2) instead of the singular "recording source".
  </action>
  <verify>
    <automated>grep -q 'source_kind' gtm-coach-pro/references/mcp-discovery.md && grep -q 'drive_folder' gtm-coach-pro/references/mcp-discovery.md && grep -q 'config_schema_version' gtm-coach-pro/references/mcp-discovery.md && grep -q 'recording_sources' gtm-coach-pro/references/mcp-discovery.md && grep -q 'file_id' gtm-coach-pro/references/mcp-discovery.md && grep -q 'root_folder_id' gtm-coach-pro/references/mcp-discovery.md && grep -Eq 'Notes by Gemini|Google Drive' gtm-coach-pro/CONNECTORS.md && grep -q 'drive_folder' gtm-coach-pro/skills/gtm-coach/SKILL.md && grep -q 'recording_sources' gtm-coach-pro/references/memory-bank.md && echo TRACER_SEAM_OK</automated>
  </verify>
  <acceptance_criteria>
    - GREP (mcp-discovery.md): contains `source_kind`, both literal values `api` and `drive_folder`, `config_schema_version`, `recording_sources`, `id_field` paired with `file_id`, and `root_folder_id`.
    - GREP (CONNECTORS.md): contains `Notes by Gemini` or `Google Drive` in a `~~meeting recording` context, described as a document store / doc-store.
    - GREP (gtm-coach/SKILL.md): Step 2 contains `drive_folder`.
    - GREP (memory-bank.md): config.json layout line contains `recording_sources`.
    - BEHAVIOR (tool-agnostic, DISC-01): the Drive binding text uses capability-probe language (probe / capability / `tool_map`) and names no single Drive tool as a *required* binding key — any Drive tool names present read as non-exhaustive examples of the shape to map, exactly as §1 already treats vendor tool-name fragments.
    - BEHAVIOR (end-to-end, DISC-04): reading CONNECTORS.md → mcp-discovery.md → gtm-coach Step 2 → memory-bank.md in order, an executor can trace a single connected Drive tool being bound to `~~meeting recording` with `source_kind: drive_folder` and persisted as one `recording_sources[]` entry with a resolved `root_folder_id` — with no step depending on the folder ladder, migration, or privacy re-surface still to come.
  </acceptance_criteria>
  <done>The `source_kind` + `recording_sources[]` v2 + capability-probe seam exists coherently across all four files for one Drive folder, committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Drive documents → local `sales-memory/` bank | Drive docs (potentially shared, may contain third-party comments and non-buyer speaker names) cross into the local bank via the connected Drive MCP tool |
| `config.json` persisted mapping → every skill | the persisted `recording_sources[]` shape controls which folder is read and how a source is treated; a malformed seam mis-routes ingestion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | `recording_sources[]` v2 seam / config schema | medium | mitigate | Additive array shape carrying explicit `source_kind`; the non-breaking migration that preserves existing `tool_map` is specified in Plan 02 |
| T-01-SC | Tampering | package installs | n/a | accept | No npm/pip/cargo installs in this phase — markdown edits only; Package Legitimacy Gate N/A |
</threat_model>

<verification>
- Run the Task 1 `<automated>` grep chain; expect `TRACER_SEAM_OK`.
- Manually confirm the end-to-end BEHAVIOR criterion by reading the four files in order.
- No phase VALIDATION.md exists for Phase 1 (milestone research substitutes) — do not block on it.
</verification>

<success_criteria>
The seam is provable end to end: a single Drive folder is bound as `source_kind: drive_folder` and
persisted into `recording_sources[]` (schema v2), traceable across CONNECTORS.md, mcp-discovery.md,
gtm-coach Step 2, and memory-bank.md — with the Drive binding done by capability probe, no hardcoded
tool name as a required key (DISC-01), and every source carrying a `source_kind` (DISC-04).
</success_criteria>

<output>
Create `.planning/phases/01-discovery-config-schema-v2/01-01-SUMMARY.md` when done.
</output>
