---
phase: 02-drive-source-pairing-parsing-contract
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - gtm-coach-pro/references/drive-source.md
autonomous: true
requirements: [PARSE-02, PARSE-03]
must_haves:
  truths:
    - "Given a notes doc's Summary/Details/Next-steps content, the procedure extracts SPICED-schema fields by semantic role / synonym matching (not exact heading strings), and records a coverage grade per field reflecting that Summary/Next-steps map strongly while Situation/Pain/Impact/Decision are sparse (PARSE-02)"
    - "When an expected section is missing or renamed, the procedure degrades gracefully to a whole-body summary and a narrower `spiced_coverage`, and never fails the ingest (PARSE-02)"
    - "Gemini's 'Decisions' section (Aligned / Needs Further Discussion / Disagreed / Shelved) maps to meeting-outcome signals/risks, and to `critical_event` only when an outcome is explicitly tied to a date — never auto-filed into SPICED's 'Decision' (buying-process/economic-buyer/paper) field (PARSE-03)"
  artifacts:
    - "gtm-coach-pro/references/drive-source.md: full semantic-role parse table — synonym sets per section (Summary/Overview, Details/Discussion, Decisions, Next steps/Action items, Attendees/Participants) → target SPICED fields, with a coverage grade (HIGH/SPARSE) per field"
    - "gtm-coach-pro/references/drive-source.md: graceful-degradation rule — no heading matches a known synonym set → whole-body summary into `## Summary`, narrower `spiced_coverage`, ingest continues (never fails)"
    - "gtm-coach-pro/references/drive-source.md: Decisions-vs-Decision disambiguation section — Gemini 'Decisions' → `## Signals` meeting-outcome signals/risks (and dated `critical_event`), explicitly NOT SPICED 'Decision'; Gemini's status labels kept labeled as Gemini's own inference"
  key_links:
    - "The parse table's target field names (`## Summary`, `## SPICED captured this call`, `## Signals`, `## Commitments & next steps`, `next_step`, `attendees_internal`/`attendees_external`, `spiced_coverage`) must match memory-bank.md — drift orphans the parse from the shared write path"
    - "The graceful-degradation `spiced_coverage` narrowing must be the same field call-prep/pipeline-review read as expected per-source variance, so a sparse Drive call is not mis-flagged as under-qualified"
    - "The Decisions section must route to `## Signals`/risks and never to the SPICED `decision` field — a naive name-match here silently misfiles meeting-outcome alignment as buying-process signal"
---

<objective>
Harden the notes-doc parse the tracer proved: replace its minimal happy-path map with the full
semantic-role/synonym table and per-field coverage grades, add the graceful-degradation rule that
keeps an ingest alive when a section is missing or renamed, and add the load-bearing
Decisions-vs-Decision disambiguation so Gemini's meeting-outcome section never gets misfiled as
SPICED's buying-process field.

Purpose: make the parse resilient to Google's evolving, un-versioned doc template (which has already
gained a "Decisions" section once) and correct about the one naming collision that would otherwise
corrupt SPICED data.
Output: full parse table + coverage grades + graceful-degradation rule + Decisions disambiguation,
appended to / expanding the parse section `drive-source.md` already carries.
Requirements: PARSE-02 (role-based parse + graceful degradation), PARSE-03 (Decisions disambiguation).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Markdown skill plugin — no build/test. Acceptance is grep-style source assertions plus
procedure-level behavior. No phase RESEARCH.md/VALIDATION.md exists for Phase 2 by design (milestone
research in `.planning/research/` substitutes) — do not block on its absence.
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/02-drive-source-pairing-parsing-contract/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full semantic-role parse table + coverage grades + graceful degradation (PARSE-02)</name>
  <files>gtm-coach-pro/references/drive-source.md</files>
  <read_first>
    @gtm-coach-pro/references/drive-source.md (the minimal happy-path parse section the tracer wrote in Plan 01 — this task expands it into the full table; do not duplicate the intro/export sections)
    @.planning/research/FEATURES.md (the "SPICED Field Mapping — What Gemini Notes Can and Can't Populate" table: Summary HIGH, Next-steps HIGH, Situation/Pain/Impact SPARSE, Decision SPARSE, attendees inferred by domain; and the "Net effect" note that sparse SPICED coverage is real per-source variance, not a parse defect)
    @.planning/research/PITFALLS.md (Pitfall 1 brittle heading parsing + "How to avoid": semantic-role/synonym matching, whole-body fallback, never fail the ingest; the localized-headings and template-drift warnings)
    @.planning/research/ARCHITECTURE.md (Pattern 3 extraction contract with graceful degradation; Anti-Pattern 3 one rigid parser)
    @gtm-coach-pro/references/memory-bank.md (target names: `## Summary`, `## SPICED captured this call`, `## Signals`, `## Commitments & next steps`, `next_step`, `attendees_internal`/`attendees_external`, `spiced_coverage`)
  </read_first>
  <action>
    Expand the parse section of `drive-source.md` from the tracer's happy-path map into the full
    contract. Add a semantic-role parse table that matches sections by a set of SYNONYMS per role — not
    an exact heading string — and names each role's target SPICED field(s):
    Summary/Overview → `## Summary` (narrative recap); Details/Discussion → the sparse SPICED fields
    Situation / Pain / Impact, inferred from prose; Next steps/Action items → `## Commitments & next
    steps` + `next_step`; Attendees/Participants → `attendees_internal`/`attendees_external`, split by
    matching participant names/emails against the user's own domain (internal) vs. everything else
    (external). Give each SPICED field a coverage grade drawn from FEATURES.md — Summary and Next-steps
    HIGH; Situation, Pain, Impact, Decision SPARSE — and state plainly that thinner Situation/Pain/
    Impact/Decision coverage is expected source variance a `Notes by Gemini` doc legitimately has, NOT
    a parse defect, so `spiced_coverage` for these calls is legitimately shorter and downstream
    SPICED-gap detection (call-prep, pipeline-review) should treat it as expected-by-source, not flag
    every Drive call as under-qualified.

    State the matching rule explicitly: match on loose synonyms of the heading text and on the
    list/paragraph structure that follows a heading, tolerating reordered sections, added sections, and
    localized (non-English) headings — never a fixed heading vocabulary.

    Add a graceful-degradation rule as its own subsection: if NO heading matches any known synonym set
    (Google changed the template, or it's an unusual meeting type / locale), do not fail the ingest —
    dump the whole exported doc body into the call file's `## Summary`, extract whatever SPICED
    elements can be inferred from the prose, and record a NARROWER `spiced_coverage` list so the gap is
    visible rather than silently assumed complete. State that a call must ingest with partial SPICED
    coverage rather than error, matching the plugin's existing "prefer transcripts; fall back to
    summaries" fallback posture, and that what wasn't found is noted (not silently dropped).

    Do NOT restate the export or pairing steps here (those are their own sections); this task owns the
    parse table, coverage grades, and degradation rule only.
  </action>
  <verify>
    <automated>grep -Eiq 'synonym' gtm-coach-pro/references/drive-source.md && grep -Eiq 'coverage' gtm-coach-pro/references/drive-source.md && grep -q 'spiced_coverage' gtm-coach-pro/references/drive-source.md && grep -Eiq 'whole.?body|whole doc|entire doc' gtm-coach-pro/references/drive-source.md && grep -Eiq 'never fail|does not fail|not fail the ingest|continue' gtm-coach-pro/references/drive-source.md && grep -Eiq 'attendees_internal|attendees_external' gtm-coach-pro/references/drive-source.md && grep -Eiq 'expected|variance' gtm-coach-pro/references/drive-source.md && ! grep -qiE 'files\.export|files\.list|drive\.files' gtm-coach-pro/references/drive-source.md && echo PARSE_TABLE_OK</automated>
  </verify>
  <acceptance_criteria>
    - GREP: the parse section contains `synonym` (matching rule), `coverage` + `spiced_coverage` (coverage grades), whole-body-fallback language (`whole body`/`whole doc`/`entire doc`), non-failing language (`never fail`/`not fail the ingest`/`continue`), `attendees_internal`/`attendees_external`, and `expected`/`variance` (sparse coverage is expected per-source variance).
    - BEHAVIOR (PARSE-02, role matching): the table maps sections to SPICED fields by synonym + following structure, tolerates reordered/added/localized headings, and never depends on an exact heading string.
    - BEHAVIOR (PARSE-02, graceful degradation): a doc with no matching headings still ingests — whole body → `## Summary`, narrower `spiced_coverage`, ingest continues — and a sparse SPICED result is documented as expected source variance, not a defect that mis-flags the call as under-qualified.
    - GREP (tool-agnostic guard): still no vendor REST literals (`files.export`, `files.list`, `drive.files`) anywhere in the doc.
  </acceptance_criteria>
  <done>The parse section carries the full synonym-based SPICED role table with coverage grades and a graceful-degradation rule that keeps an ingest alive with partial coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Decisions-vs-Decision disambiguation (PARSE-03)</name>
  <files>gtm-coach-pro/references/drive-source.md</files>
  <read_first>
    @gtm-coach-pro/references/drive-source.md (the parse table Task 1 just added — the Decisions row/section slots alongside it; `## Signals` is the target)
    @.planning/research/FEATURES.md (the Differentiators "Decisions section → partial SPICED signal" row and the SPICED-mapping "Decision" row: the naming collision — Gemini "Decisions" = meeting-outcome alignment; SPICED "Decision" = buying process/economic buyer/paper — map Gemini Decisions → signals/risks, and to `critical_event` only when tied to a date; do not auto-populate SPICED Decision without a procurement-language text match)
    @.planning/research/PITFALLS.md (Pitfall 7 "Do not let Gemini's own inferred structure (Decisions Aligned/Disagreed labels) get presented as the rep's or buyer's own assessment — keep it labeled as Gemini's inference")
    @gtm-coach-pro/references/spiced-framework.md (what SPICED's "Decision" element actually means, to contrast against Gemini's "Decisions" section)
  </read_first>
  <action>
    Add a dedicated disambiguation section to `drive-source.md` covering the one naming collision that
    would corrupt SPICED data. State the two distinct concepts explicitly: Gemini's "Decisions" section
    records MEETING-OUTCOME alignment with a status taxonomy (Aligned / Needs Further Discussion /
    Disagreed / Shelved); SPICED's "Decision" field is the BUYING PROCESS — decision criteria, economic
    buyer, paper/legal/security steps. They are not the same thing and must never be auto-mapped onto
    each other.

    Specify the mapping: content from Gemini's "Decisions" section routes to the call file's
    `## Signals` as meeting-outcome signals/risks (e.g. a "Disagreed"/"Shelved" item is a risk; an
    "Aligned" item is a positive signal), and to `critical_event` ONLY when a decision is explicitly
    tied to a date/deadline/renewal. It is NEVER auto-filed into the SPICED `decision` field — populate
    SPICED Decision only from an actual text match on procurement/buying-process language
    (economic buyer, criteria, legal/security/paper), same as any source without a dedicated
    buying-process field. State that Gemini's status labels are kept labeled as Gemini's own inference
    layer — not presented as the rep's or the buyer's own assessment.

    This section owns only the Decisions disambiguation; do not restate the general parse table.
  </action>
  <verify>
    <automated>grep -Eiq 'Aligned' gtm-coach-pro/references/drive-source.md && grep -Eiq 'Shelved|Disagreed|Needs Further Discussion' gtm-coach-pro/references/drive-source.md && grep -Eiq 'meeting.outcome|outcome' gtm-coach-pro/references/drive-source.md && grep -Eiq 'signals|risks' gtm-coach-pro/references/drive-source.md && grep -Eiq 'critical_event' gtm-coach-pro/references/drive-source.md && grep -Eiq 'buying.process|economic buyer|procurement' gtm-coach-pro/references/drive-source.md && grep -Eiq 'never|not.*Decision field|not.*SPICED' gtm-coach-pro/references/drive-source.md && echo DECISIONS_DISAMBIG_OK</automated>
  </verify>
  <acceptance_criteria>
    - GREP: the section names Gemini's status taxonomy (`Aligned` + at least one of `Shelved`/`Disagreed`/`Needs Further Discussion`), the `outcome`/`meeting-outcome` framing, `signals`/`risks` and `critical_event` as the targets, the `buying-process`/`economic buyer`/`procurement` contrast, and explicit negation language keeping it out of the SPICED Decision field.
    - BEHAVIOR (PARSE-03): reading the section, Gemini's "Decisions" content maps to `## Signals` meeting-outcome signals/risks (and dated `critical_event`), is explicitly kept OUT of the SPICED `decision` field unless procurement-language text matches, and Gemini's Aligned/Disagreed labels are labeled as Gemini's inference — not the buyer's/rep's own words.
  </acceptance_criteria>
  <done>A disambiguation section maps Gemini's "Decisions" to meeting-outcome signals/risks and explicitly bars it from SPICED's "Decision" field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini notes-doc sections → SPICED call fields | a naive heading-string parser mis-maps or silently drops fields when Google reorders/renames/localizes sections |
| Gemini "Decisions" section → SPICED "Decision" field | a name-match collision misfiles meeting-outcome alignment as buying-process signal, corrupting deal qualification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | notes-doc parse mapping (Task 1) | medium | mitigate | Synonym/role matching with whole-body graceful degradation and honest `spiced_coverage` narrowing — a template change degrades coverage visibly rather than silently corrupting fields |
| T-02-04 | Tampering | Decisions → SPICED mapping (Task 2) | high | mitigate | Explicit disambiguation routes Gemini "Decisions" to `## Signals`/risks and dated `critical_event`, and bars auto-fill of SPICED `decision` without a procurement-language text match |
| T-02-SC | Tampering | package installs | n/a | accept | No npm/pip/cargo installs — markdown edits only; Package Legitimacy Gate N/A |
</threat_model>

<verification>
- Run each task's `<automated>` grep chain; expect `PARSE_TABLE_OK`, `DECISIONS_DISAMBIG_OK`.
- Confirm the BEHAVIOR criteria by reading the expanded parse section + the disambiguation section.
- No phase RESEARCH.md/VALIDATION.md exists for Phase 2 by design — do not block on it.
</verification>

<success_criteria>
The parse contract is drift-resistant and correct: sections are matched by synonym/role with per-field
coverage grades and a graceful-degradation-to-whole-body-summary rule that never fails an ingest
(PARSE-02), and Gemini's "Decisions" section is explicitly disambiguated from SPICED's "Decision"
field, routing to meeting-outcome signals/risks instead (PARSE-03).
</success_criteria>

<output>
Create `.planning/phases/02-drive-source-pairing-parsing-contract/02-02-SUMMARY.md` when done.
</output>
