---
phase: 02-drive-source-pairing-parsing-contract
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - gtm-coach-pro/references/drive-source.md
autonomous: true
requirements: [PARSE-04, TRUST-01]
must_haves:
  truths:
    - "Given a notes doc with exactly one unambiguous transcript candidate, the procedure pairs them automatically (single-doc, new-model subfolder co-location, or legacy filename+date-window proximity); given more than one plausible candidate, it flags the ambiguous set to the user instead of guessing (PARSE-04)"
    - "The notes doc is the primary record and the transcript is optional enrichment — a call ingests with zero, one, or an ambiguous set of transcript candidates; the paired transcript file id (or null) is recorded for auditability, and pairing never blocks the call (PARSE-04)"
    - "The resulting call record marks transcript-sourced text as verbatim (quotable buyer language) and notes-doc content as paraphrase (narrative only), gated by `has_transcript`, so battlecards / playbook-builder / voice-of-customer never present Gemini's AI summary as an exact buyer quote (TRUST-01)"
  artifacts:
    - "gtm-coach-pro/references/drive-source.md: full transcript-pairing heuristic — ordered cases (1) embedded/single-doc, (2) new-model per-meeting subfolder co-location, (3) legacy flat-folder longest-common-prefix title after stripping ' Notes by Gemini' + createdTime within ~24h; (4) unresolved/ambiguous → flag to user, `transcript_doc_id: null`/`has_transcript: false`, never block"
    - "gtm-coach-pro/references/drive-source.md: ambiguity rule — >1 plausible candidate requires both title AND date match; if still >1, skip auto-pairing and flag the candidate set to the user rather than guessing; record which transcript file id (if any) paired to which call id"
    - "gtm-coach-pro/references/drive-source.md: full provenance write-time contract — transcript text eligible as quoted verbatim buyer language in `## Signals`; notes-doc AI summary eligible only as narrative/paraphrase in `## Summary`/`## SPICED captured this call`; `has_transcript: false` gating; named downstream skills (battlecards, playbook-builder, voice-of-customer) must skip or caveat no-transcript calls as evidence"
  key_links:
    - "The paired `transcript_doc_id`/`has_transcript` fields must match memory-bank.md frontmatter + index.json so Phase 4's write path and the consuming skills read the same provenance flag"
    - "The provenance write-rule (verbatim vs. paraphrase) must reference the same `## Signals` vs. `## Summary`/`## SPICED captured this call` split the parse table (Plan 02) writes into, or provenance and parse disagree on where quotes land"
    - "The dedup identity stays the notes-doc file id (Plan 01 `## Dedup key`); the transcript file id is a secondary paired artifact, never the call id — keying dedup on the transcript would break identity"
---

<objective>
Complete the phase: replace the tracer's happy-path pairing with the full transcript-pairing
heuristic (single-doc, new-model subfolder, legacy filename+date-window, and ambiguous-set flagging),
and expand the tracer's core provenance tag into the full write-time contract that keeps Gemini's AI
paraphrase from ever being presented as an exact buyer quote by the three downstream skills that
promise verbatim language.

Purpose: close the two highest-risk gaps the milestone research flags — notes/transcript mispairing
(recurring meetings, renamed docs, back-to-backs) and conflating AI summary with verbatim buyer
language — so Drive-sourced calls are evidence-first, not second-class.
Output: full pairing heuristic + ambiguity rule + full provenance contract, expanding the pairing and
provenance sections `drive-source.md` already carries.
Requirements: PARSE-04 (pairing + ambiguity flagging), TRUST-01 (provenance).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Markdown skill plugin — no build/test. Acceptance is grep-style source assertions plus
procedure-level behavior. No phase RESEARCH.md/VALIDATION.md exists for Phase 2 by design (milestone
research in `.planning/research/` substitutes) — do not block on its absence.
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/02-drive-source-pairing-parsing-contract/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full transcript-pairing heuristic + ambiguity flagging (PARSE-04)</name>
  <files>gtm-coach-pro/references/drive-source.md</files>
  <read_first>
    @gtm-coach-pro/references/drive-source.md (the subfolder happy-path pairing section the tracer wrote in Plan 01 — this task expands it into the full ordered heuristic; keep notes-doc-primary framing consistent)
    @.planning/research/ARCHITECTURE.md (Pattern 2 "Notes/transcript pairing" — the ordered pairing procedure: (1) single-doc embedded transcript, (2) new-model per-meeting subfolder co-location, (3) legacy flat-folder longest-common-prefix title after stripping ' - Notes by Gemini' + createdTime within ~24h, (4) unresolved → transcript_doc_id null / has_transcript false, never block)
    @.planning/research/PITFALLS.md (Pitfall 2 mispairing "How to avoid": pair on strongest structured signal; if only weak signals, require BOTH title AND date match; if >1 candidate pair in a window, skip auto-pairing and flag the ambiguous set; notes doc primary / transcript optional; record which transcript file id paired to which call id for auditability. Also the "Looks Done But Isn't" recurring-meeting title-collision check)
    @gtm-coach-pro/references/memory-bank.md (`has_transcript` frontmatter; index.json.calls[] where the paired transcript file id is recorded — architecture calls it `transcript_doc_id`)
  </read_first>
  <action>
    Expand the pairing section of `drive-source.md` from the tracer's subfolder happy path into the
    full ordered heuristic. Document the cases tried in order, stopping at the first match:
    (1) single-doc / embedded case — the notes doc itself contains an embedded transcript section, in
    which case `transcript_doc_id = notes_doc_id`; (2) new-model subfolder case (default going forward)
    — the notes doc's parent is a per-meeting subfolder, so a sibling whose name or type indicates a
    transcript pairs by co-location alone; (3) legacy flat-folder case — search the same parent folder
    for a file whose title shares the longest common prefix with the notes doc's title (after stripping
    the ` - Notes by Gemini` suffix) and whose `createdTime` falls within ~24h of the notes doc's
    `createdTime`; (4) unresolved — proceed with `transcript_doc_id: null` and `has_transcript: false`.

    Add the ambiguity rule explicitly: pair on the strongest structured signal the connected Drive tool
    actually returns (shared parent folder first), NOT filename text alone. When only weak signals are
    available (title + same-day date), require BOTH a title match AND a date match; and if more than
    one plausible candidate pair exists in the window (recurring meetings with identical titles,
    renamed docs, same-day back-to-backs), do NOT auto-pair — flag the ambiguous candidate set to the
    user and let them choose, rather than guessing. State that the notes doc is the primary record and
    the transcript is optional enrichment: a call must ingest correctly with zero, one, or an ambiguous
    set of transcript candidates, and pairing must NEVER block the whole call. Record which transcript
    file id (if any) paired to which call id (in call frontmatter / `index.json`) so a mispairing is
    auditable and correctable later rather than silently baked in.

    Keep the dedup identity as the notes-doc file id (Plan 01's `## Dedup key`); the transcript file id
    is a secondary paired artifact and is never the call id. This task owns pairing only — do not
    restate the parse table or the provenance write-rule (Task 2).
  </action>
  <verify>
    <automated>grep -Eiq 'subfolder|co-locat' gtm-coach-pro/references/drive-source.md && grep -Eiq 'legacy' gtm-coach-pro/references/drive-source.md && grep -Eiq 'longest common prefix|common prefix|strip' gtm-coach-pro/references/drive-source.md && grep -Eiq 'createdTime|24h|date window|date match' gtm-coach-pro/references/drive-source.md && grep -Eiq 'ambigu' gtm-coach-pro/references/drive-source.md && grep -Eiq 'flag' gtm-coach-pro/references/drive-source.md && grep -Eiq 'transcript_doc_id' gtm-coach-pro/references/drive-source.md && grep -q 'has_transcript' gtm-coach-pro/references/drive-source.md && grep -Eiq 'primary|never block|optional' gtm-coach-pro/references/drive-source.md && echo PAIRING_OK</automated>
  </verify>
  <acceptance_criteria>
    - GREP: the pairing section contains subfolder/co-location language, `legacy` (flat-folder case), prefix/strip language, a date-window signal (`createdTime`/`24h`/`date match`), `ambigu*` + `flag` (ambiguity flagging), `transcript_doc_id` + `has_transcript`, and primary/optional/never-block framing.
    - BEHAVIOR (PARSE-04, unambiguous): one notes doc with exactly one transcript candidate pairs automatically via the first matching case (embedded → subfolder → legacy title+date).
    - BEHAVIOR (PARSE-04, ambiguous): >1 plausible candidate requires both title AND date match, and if still >1 the candidate set is flagged to the user, not guessed; the call still ingests with `transcript_doc_id: null`/`has_transcript: false` if unresolved, and the paired file id (or null) is recorded for auditability.
    - BEHAVIOR (identity): the notes-doc file id stays the call id; the transcript file id is never used as the dedup key.
  </acceptance_criteria>
  <done>The pairing section covers all four ordered cases plus the both-title-and-date ambiguity rule that flags rather than guesses, with the notes doc primary and pairing never blocking ingest.</done>
</task>

<task type="auto">
  <name>Task 2: Full provenance write-time contract (TRUST-01)</name>
  <files>gtm-coach-pro/references/drive-source.md</files>
  <read_first>
    @gtm-coach-pro/references/drive-source.md (the core verbatim/paraphrase tag the tracer wrote in Plan 01, and the pairing/`has_transcript` outcome Task 1 just finalized — this task turns the core tag into the full write-time contract)
    @.planning/research/PITFALLS.md (Pitfall 7 "Conflating Gemini's AI summary with verbatim buyer language" — the full "How to avoid": transcript text eligible as quoted buyer statement in `## Signals`; notes-doc AI summary eligible only as narrative/paraphrase in `## Summary`/`## SPICED captured this call`, never a direct quote; when transcript missing/unpaired mark `has_transcript: false` and have downstream skills skip or caveat; keep Gemini's Decisions labels as Gemini's inference. Also the "Evidence provenance" Looks-Done-But-Isn't and UX-pitfall rows)
    @.planning/research/FEATURES.md (Competitor analysis — transcript availability is the difference that makes Drive calls need explicit provenance; the Core-Value evidence-first framing)
    @gtm-coach-pro/references/memory-bank.md (`has_transcript` field; `## Signals` vs `## Summary`/`## SPICED captured this call` split)
    @gtm-coach-pro/skills/battlecards (skill dir), @gtm-coach-pro/skills/playbook-builder (skill dir), @gtm-coach-pro/skills/voice-of-customer (skill dir) — the three consuming skills that promise "exact buyer language" and must respect the provenance flag; name them by skill, do not modify them this phase
  </read_first>
  <reversibility rating="costly">The verbatim-vs-paraphrase write rule and the `has_transcript` provenance gate are baked into every Drive-sourced call record and consumed by battlecards / playbook-builder / voice-of-customer. Research locked the rule (Pitfall 7); changing the eligibility semantics later means re-auditing every generated artifact. Flagged for care, not gated.</reversibility>
  <action>
    Expand the provenance section of `drive-source.md` from the tracer's core tag into the full
    write-time contract. State the eligibility rule precisely: text pulled from the TRANSCRIPT doc
    (verbatim, speaker-attributed) is the ONLY text eligible to be written as a quoted buyer statement
    (a `"…"` quote with attribution) in the call file's `## Signals`, and thus the only text
    downstream skills may later consume as "exact buyer language." Text pulled from the NOTES doc is
    Gemini's AI paraphrase — eligible only as narrative/summary in `## Summary` and
    `## SPICED captured this call`, and NEVER presented as a direct quote.

    Specify the missing-transcript case: when the transcript is absent or unpaired (from Task 1's
    heuristic — `has_transcript: false`), mark the call record accordingly and state that downstream
    skills which promise verbatim language must either skip that call as an evidence source or clearly
    caveat it as summary-derived. Name the three consuming skills explicitly — `battlecards`,
    `playbook-builder`, `voice-of-customer` — as the ones that must respect the `has_transcript` /
    provenance flag rather than treating all `## Signals` text as equally quotable. (Do NOT modify
    those skill files in this phase — the phase deliverable is the contract in `drive-source.md`; wiring
    the skills to honor it is downstream.)

    Add the reinforcing rule that Gemini's own inferred structure (the Decisions section's
    Aligned/Disagreed labels from Plan 02) is kept labeled as Gemini's inference, never presented as the
    rep's or buyer's own assessment. Close by tying provenance back to the plugin's evidence-first core
    value: a Drive notes-only call is legitimately weaker evidence than a transcript-backed one, and the
    contract makes that visible rather than laundering paraphrase as a quote.

    This task owns the provenance contract only — do not restate the pairing heuristic or the parse
    table.
  </action>
  <verify>
    <automated>grep -iq 'verbatim' gtm-coach-pro/references/drive-source.md && grep -iq 'paraphrase' gtm-coach-pro/references/drive-source.md && grep -q 'has_transcript' gtm-coach-pro/references/drive-source.md && grep -Eiq 'quote|quoted' gtm-coach-pro/references/drive-source.md && grep -q '## Signals' gtm-coach-pro/references/drive-source.md && grep -Eiq 'battlecards' gtm-coach-pro/references/drive-source.md && grep -Eiq 'playbook-builder|playbook builder' gtm-coach-pro/references/drive-source.md && grep -Eiq 'voice-of-customer|voice of customer' gtm-coach-pro/references/drive-source.md && grep -Eiq 'skip|caveat' gtm-coach-pro/references/drive-source.md && ! grep -qiE 'files\.export|files\.list|drive\.files' gtm-coach-pro/references/drive-source.md && echo PROVENANCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - GREP: the provenance section contains `verbatim` + `paraphrase`, `has_transcript`, quote/quoted language, `## Signals`, all three consuming skill names (`battlecards`, `playbook-builder`, `voice-of-customer`), and skip/caveat language for no-transcript calls.
    - BEHAVIOR (TRUST-01): only transcript text is eligible as a quoted buyer statement in `## Signals`; notes-doc text is narrative/paraphrase only in `## Summary`/`## SPICED captured this call` and never a direct quote; when `has_transcript: false`, the three named skills must skip or caveat the call rather than quote it — so no downstream skill can present Gemini's AI summary as an exact buyer quote.
    - BEHAVIOR (labeling): Gemini's Decisions status labels are kept labeled as Gemini's inference, not the buyer's/rep's own assessment.
    - GREP (tool-agnostic guard): still no vendor REST literals (`files.export`, `files.list`, `drive.files`) anywhere in the doc.
  </acceptance_criteria>
  <done>The provenance contract makes transcript text the only quotable buyer language, gates no-transcript calls with `has_transcript`, and names the three downstream skills that must honor it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Candidate transcript docs → the call's paired transcript | mispairing (recurring/renamed/back-to-back meetings) attributes the wrong buyer's words to a call |
| Notes-doc AI paraphrase → downstream "exact buyer language" (battlecards / playbook-builder / voice-of-customer) | without a provenance gate, tidy AI summary text is laundered as a verbatim quote, undermining the evidence-first core value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Spoofing | transcript pairing (Task 1) | high | mitigate | Pair on strongest structured signal; require both title AND date on weak signals; flag ambiguous sets to the user instead of guessing; record the paired transcript id for audit; notes doc stays the identity so a bad pair never corrupts dedup |
| T-02-06 | Spoofing | provenance write-rule (Task 2) | high | mitigate | Only transcript text is quotable in `## Signals`; notes text is paraphrase-only; `has_transcript: false` forces battlecards/playbook-builder/voice-of-customer to skip or caveat — AI summary cannot masquerade as an exact buyer quote |
| T-02-SC | Tampering | package installs | n/a | accept | No npm/pip/cargo installs — markdown edits only; Package Legitimacy Gate N/A |
</threat_model>

<verification>
- Run each task's `<automated>` grep chain; expect `PAIRING_OK`, `PROVENANCE_OK`.
- Confirm the BEHAVIOR criteria by reading the expanded pairing + provenance sections.
- End-to-end read-through: `drive-source.md` now covers detect → export → parse (full table +
  degradation) → Decisions disambiguation → pair (full heuristic + ambiguity) → provenance (full
  contract) → worked example → dedup-key note. No phase VALIDATION.md exists by design — do not block.
</verification>

<success_criteria>
The two highest-risk gaps are closed: an unambiguous transcript pairs automatically while an ambiguous
set is flagged to the user rather than guessed, with the notes doc primary and pairing never blocking
ingest (PARSE-04); and the provenance contract makes transcript text the only quotable buyer language,
gates no-transcript calls with `has_transcript`, and binds battlecards / playbook-builder /
voice-of-customer to honor it so Gemini's AI summary is never presented as an exact buyer quote
(TRUST-01).
</success_criteria>

<output>
Create `.planning/phases/02-drive-source-pairing-parsing-contract/02-03-SUMMARY.md` when done.
</output>
